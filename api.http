@api_url = http://localhost:3000/api

###
# --- CRUD Básico ---

### 1. Obtener todos los productos
# Devuelve la lista completa de productos.
GET {{api_url}}/productos
Accept: application/json

### 2. Obtener un producto por su código
GET {{api_url}}/productos/1478
Accept: application/json

### 2.1 Obtener un producto con un código inexistente (debería dar 404)
GET {{api_url}}/productos/1000
Accept: application/json

### 3. Crear un nuevo producto
POST {{api_url}}/productos
Content-Type: application/json

{
    "codigo": 1000,
    "nombre": "Teclado Luminoso RGB",
    "precio": 65.50,
    "categoria": ["Accesorios", "Computación", "Gaming"]
}

### 3.1 Intentar crear un producto con código repetido (debería dar error 400)
POST {{api_url}}/productos
Content-Type: application/json

{
    "codigo": 1478,  
    "nombre": "Producto Duplicado",
    "precio": 10,
    "categoria": ["Test"]
}

### 3.2 Intentar crear un producto con cuerpo inválido (debería dar error 400)
POST {{api_url}}/productos
Content-Type: application/json

{
    "nombre": "Producto sin código",
    "precio": 10,
    "categoria": ["Test"]
}

### 4. Modificar un producto existente
PUT {{api_url}}/productos/1000
Content-Type: application/json

{
    "precio": 950.50
}

### 4.1 Modificar un producto inexistente (debería dar error 404)
PUT {{api_url}}/productos/1
Content-Type: application/json

{
    "precio": 12
}

### 5. Eliminar un producto
DELETE {{api_url}}/productos/1000
Accept: application/json


###
# --- Endpoints Adicionales ---

### 6. Buscar productos por término
# Busca productos que contengan 'Laptop' en su nombre.
GET {{api_url}}/productos/buscar?q=Laptop
Accept: application/json

### 6.1 Buscar con término inexistente
# Busca productos con término que no exista en ningún nombre.
GET {{api_url}}/productos/buscar?q=Heladera
Accept: application/json

### 6.2 Buscar sin término (q vacío)
# No pasa parámetro de búsqueda
GET {{api_url}}/productos/buscar
Accept: application/json

### 7. Filtrar productos por categoría
# Busca productos que pertenezcan a la categoría 'Accesorios'.
GET {{api_url}}/productos/categoria/Accesorios
Accept: application/json

### 7.1 Filtrar productos por categoría - Categoría no encontrada
GET {{api_url}}/productos/categoria/CategoriaInexistente
Accept: application/json

### 8. Filtrar productos por rango de precio
# Busca productos con precios entre 100 y 500.
GET {{api_url}}/productos/precio/100-500
Accept: application/json

### 8.1. Buscar productos en un rango sin coincidencias
GET {{api_url}}/productos/precio/10-15
Accept: application/json

### 9. Carga masiva de productos
# Agrega múltiples productos en una sola petición.
POST {{api_url}}/productos/masivo
Content-Type: application/json

[
    {
        "codigo": 201,
        "nombre": "Monitor Gamer 27 pulgadas",
        "precio": 399.99,
        "categoria": ["Monitores", "Computación", "Gaming"]
    },
    {
        "codigo": 202,
        "nombre": "Silla Gamer Ergonómica",
        "precio": 249.50,
        "categoria": ["Mobiliario", "Gaming"]
    }
]